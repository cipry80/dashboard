import Navigation from './Navigation';

const Header: React.FunctionComponent = props => {
    return (
        <header>
            <Navigation />
        </header>
    );
};

export default Header;
