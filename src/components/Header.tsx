import { PrimaryNavigation } from './Navigation';

const Header: React.FunctionComponent = () => {
    return (
        <header>
            <PrimaryNavigation />
        </header>
    );
};

export default Header;
