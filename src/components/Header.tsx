import { TopNavigation } from './Navigation';

const Header: React.FunctionComponent = () => {
    return (
        <header>
            <TopNavigation />
        </header>
    );
};

export default Header;
